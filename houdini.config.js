/// <references types="houdini-svelte">

/** @type {import('houdini').ConfigFile} */
const config = {
    "schemaPath": "path/to/src/lib/**/*.graphql",
    "plugins": {
        "houdini-svelte": {}
    }
}

export default config
